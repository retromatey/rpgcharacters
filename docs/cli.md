# CLI Reference

```bash
{{ run("rpgcharacters --help") }}
```

## Example Character

!!! tip 
    Use `--seed` when writing tests or examples.

```bash
rpgcharacters --non-interactive --seed 42
```

```bash
{{ run("rpgcharacters --non-interactive --seed 42") }}
```
